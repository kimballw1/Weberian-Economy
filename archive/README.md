# Archive

Earlier, exploratory versions of the analysis (the original `OG index OLS`
notebooks). They are kept for provenance only and are **superseded by the clean,
documented notebooks in `../notebooks/`**.

These read from older data files and use absolute paths; they are not maintained
and may not run as-is. Use `../notebooks/` for the current, reproducible pipeline.
